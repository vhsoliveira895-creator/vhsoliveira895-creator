# Git

Nível: iniciante. Versiono o que estou estudando. Ainda praticando o fluxo do dia a dia.

Comandos que já usei:

```bash
git status
git add .
git commit -m "mensagem clara do porquê da mudança"
git log --oneline
```

O que ainda estou firmando: branches, pull request e resolver conflito com calma — sem fingir que já domino rebase avançado.

Este perfil vive no repositório especial do GitHub: [vhsoliveira895-creator/vhsoliveira895-creator](https://github.com/vhsoliveira895-creator/vhsoliveira895-creator).
